apiVersion: v1
kind: Service
metadata:
  name: {{ include "onlineboutique.fullname" . }}
  labels:
    {{- include "onlineboutique.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "onlineboutique.selectorLabels" . | nindent 4 }}
